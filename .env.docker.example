# ===========================================
# Docker Compose 环境变量配置
# ===========================================
# 复制此文件为 .env 并修改配置
# cp .env.docker.example .env
# ===========================================

# MySQL 配置
MYSQL_ROOT_PASSWORD=root123

# JWT 密钥（生产环境请修改为随机生成的密钥）
JWT_SECRET=your-256-bit-secret-key-for-jwt-token-generation-must-be-long-enough

# Session 密钥（生产环境请修改为随机生成的密钥）
SESSION_SECRET=sakura-blog-admin-secret-key-2025-very-secure

# API 地址（生产环境请修改为实际域名）
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
