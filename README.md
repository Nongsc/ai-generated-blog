# 🌸 樱花博客系统

一个现代化的全栈博客系统，采用前后端分离架构，包含管理后台和博客前台。

## 📋 项目概览

| 项目 | 技术栈 | 端口 | 说明 |
|------|--------|------|------|
| **api** | Spring Boot 3.2.5 | 8080 | 后端 REST API 服务 |
| **admin** | Next.js 16 + React 19 | 3000 | 管理后台 |
| **blog** | Next.js 16 + React 19 | 3001 | 博客前台 |

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户浏览器                                │
└─────────────────────────────────────────────────────────────────┘
                    │                           │
                    ▼                           ▼
        ┌───────────────────┐       ┌───────────────────┐
        │   Admin 前端       │       │    Blog 前端       │
        │   (Next.js)       │       │    (Next.js)       │
        │   Port: 3000      │       │    Port: 3001      │
        └───────────────────┘       └───────────────────┘
                    │                           │
                    ▼                           ▼
        ┌─────────────────────────────────────────────────┐
        │              API 后端 (Spring Boot)              │
        │                 Port: 8080                       │
        └─────────────────────────────────────────────────┘
                    │                           │
                    ▼                           ▼
        ┌───────────────────┐       ┌───────────────────┐
        │      MySQL        │       │      Redis        │
        └───────────────────┘       └───────────────────┘
```

## 🚀 快速开始

### 环境要求

- **Node.js**: >= 18.x
- **Java**: JDK 17
- **Maven**: >= 3.8
- **MySQL**: >= 8.0
- **Redis**: >= 6.0

### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd trash-05
```

#### 2. 配置数据库

```sql
CREATE DATABASE blog_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### 3. 启动 API 后端

```bash
cd api

# 配置数据库连接 (修改 application.yml)
# 启动服务
mvn spring-boot:run
```

API 文档地址: http://localhost:8080/doc.html

#### 4. 启动 Admin 管理后台

```bash
cd admin

# 安装依赖
pnpm install

# 配置环境变量 (复制 .env.example 为 .env 并修改)
cp .env.example .env

# 启动开发服务器
pnpm dev
```

访问地址: http://localhost:3000

#### 5. 启动 Blog 博客前台

```bash
cd blog

# 安装依赖
pnpm install

# 配置环境变量
cp .env.example .env

# 启动开发服务器
pnpm dev
```

访问地址: http://localhost:3001

## 📁 项目结构

```
trash-05/
├── api/                    # Spring Boot 后端
│   ├── src/main/java/com/blog/api/
│   │   ├── config/         # 配置类
│   │   ├── controller/     # 控制器
│   │   │   ├── admin/      # 管理接口 (需认证)
│   │   │   └── blog/       # 博客接口 (公开)
│   │   ├── entity/         # 实体类
│   │   ├── service/        # 业务服务
│   │   ├── mapper/         # MyBatis Mapper
│   │   ├── dto/            # 数据传输对象
│   │   ├── security/       # 安全模块
│   │   └── exception/      # 异常处理
│   └── pom.xml
│
├── admin/                  # Next.js 管理后台
│   ├── src/
│   │   ├── app/            # App Router 页面
│   │   ├── components/     # 组件
│   │   │   ├── admin/      # 管理后台组件
│   │   │   └── ui/         # UI 组件库
│   │   ├── lib/            # 工具库
│   │   └── types/          # 类型定义
│   └── package.json
│
├── blog/                   # Next.js 博客前台
│   ├── src/
│   │   ├── app/            # App Router 页面
│   │   ├── components/     # 组件
│   │   │   ├── layout/     # 布局组件
│   │   │   ├── home/       # 首页组件
│   │   │   ├── common/     # 通用组件
│   │   │   └── ui/         # UI 组件库
│   │   ├── lib/            # 工具库
│   │   ├── contexts/       # React Context
│   │   └── types/          # 类型定义
│   └── package.json
│
├── docs/                   # 项目文档
│   ├── api/                # API 文档
│   ├── admin/              # Admin 文档
│   └── blog/               # Blog 文档
│
├── .gitignore
└── README.md
```

## 🔐 默认账号

首次运行需要通过 API 注册管理员账号：

```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123","email":"admin@example.com"}'
```

## 📖 详细文档

- [API 文档](./docs/api/README.md)
- [Admin 文档](./docs/admin/README.md)
- [Blog 文档](./docs/blog/README.md)

## 🛠️ 技术栈详情

### API (后端)

| 技术 | 版本 | 说明 |
|------|------|------|
| Spring Boot | 3.2.5 | 核心框架 |
| MyBatis Plus | 3.5.5 | ORM 框架 |
| Spring Security | - | 安全框架 |
| JWT (jjwt) | 0.12.5 | Token 认证 |
| Knife4j | 4.4.0 | API 文档 |
| MySQL | - | 数据库 |
| Redis | - | 缓存 |

### Admin / Blog (前端)

| 技术 | 版本 | 说明 |
|------|------|------|
| Next.js | 16.1.6 | React 框架 |
| React | 19.2.3 | UI 库 |
| Tailwind CSS | 4.x | 样式框架 |
| Radix UI | - | 无障碍组件 |
| Framer Motion | 12.x | 动画库 |
| react-icons | 5.x | 图标库 |

## 📝 开发规范

### Git 提交规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Type 类型：**
- `feat`: 新功能
- `fix`: 修复 Bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具相关
- `ci`: CI/CD 相关

**示例：**
```
feat(api): 添加文章搜索功能
fix(blog): 修复文章详情页 404 问题
docs(readme): 更新安装说明
```

### 分支管理

```
main        # 主分支，生产环境代码
develop     # 开发分支
feature/*   # 功能分支
hotfix/*    # 紧急修复分支
release/*   # 发布分支
```

## 📄 许可证

MIT License
